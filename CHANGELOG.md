# 更新日志

## 版本 0.0.22 (2026-07-31) 自动更新与稳定性修复

> 平台感知的自动更新检查、毛玻璃开关修复、IP 定位乱码修复、持久化存储修复。

### 已知问题
- 缓存检测点击查询时未使用缓存：缓存提示弹窗点击"查询"后直接发起网络请求，未复用已缓存的天气数据

### 新功能
- 新增自动更新检查：通过 GitHub Releases API 获取最新版本，平台感知过滤对应安装包（.exe / .apk），确保 Windows 与 Android 共享同一仓库而不互相干扰
- 更新检查逻辑：若最新 Release 中无当前平台安装包，静默忽略，不弹报错提示

### 修复
- 修复毛玻璃开关无效果：HarmonyGlass 组件未感知 `useHarmonyGlass` 设置，现已从 AppState 读取开关状态，关闭时模糊归零
- 修复授权自动定位查询乱码：IP 定位 API 响应未显式 UTF-8 解码，导致中文地名乱码，现改用 `utf8.decode(response.bodyBytes)` 显式解码
- 修复 IP 定位返回街道级地名（如"青园"）：所有定位解析器统一过滤，2字且无行政后缀的地名自动 fallback 到上级行政区
- 修复 IP 定位返回拼音/英文地名：移除 ipapi.co、ip.sb 等英文 API，仅保留 pconline 和 ip-api 中文接口
- 修复日志功能不可用：Logger 初始化时序和路径权限问题已解决，日志文件正常生成
- 修复 Windows 端每次启动数据清空：SharedPreferences 存储路径在启动时不再被重置，天气缓存、历史记录、收藏城市等持久化数据正常保留
- 修复缓存数据监测未正常运行：`_lastQueryTimestamp` 重启后正确恢复，`shouldPromptCache` 按预期返回间隔提示

---

## 版本 0.0.21 (2026-07-29) 主题系统与体验优化

> 毛玻璃主题开关、定位服务修复、缓存逻辑优化、浅色模式默认主题、Windows 检查更新复原。

### 已知问题
- 日志功能不可用：日志文件未正常生成，`enableLogging` 默认开启后仍未写入日志文件，需进一步排查 `path_provider` 路径权限和 Logger 初始化时序
- 重启后毛玻璃效果未启用：切换毛玻璃开关后重启应用，毛玻璃效果未生效，GlassCard 的 `useHarmonyGlass` 状态读取可能存在时序问题
- Windows 端每次启动数据清空：重启后天气缓存数据、历史记录、收藏城市等持久化数据全部丢失，SharedPreferences 存储路径在启动时可能被重置
- 缓存数据监测未正常运行：上次查询时间戳 `_lastQueryTimestamp` 重启后丢失，`shouldPromptCache` 始终返回 false，缓存间隔提示功能不可用

### 新功能
- 设置页新增毛玻璃开关，开启后所有 UI 采用毛玻璃效果
- 开启毛玻璃时提示重启，确认后自动重启切换主题
- Windows 端复原检查更新按钮，侧边栏添加检查更新入口，显示版本对比对话框
- 启动时直接显示上一次缓存数据，无需确认弹窗

### 优化
- 默认主题调整为浅色模式，更符合用户首次使用习惯
- 取消设置保存时的"设置已更新"提示，减少打扰
- 优化定位服务，实现双 API fallback 机制（ipapi.co HTTPS → ip-api.com HTTP）

### 改进
- 移除设置页保存时的 SnackBar 提示
- 更新日志页面在 Android 端采用卡片式布局，版本徽章 + 分类图标 + 时间线样式
- Android 端增加侧向滑动（PageView）切换功能栏，带手势跟随动画
- AppSettings 新增 `useHarmonyGlass` 字段，持久化毛玻璃配置
- ThemeProvider 默认值改为 `ThemeMode.light`，loadTheme 默认索引改为 0
- ChangelogPage 双端布局分离：Android 卡片式 / Desktop 简洁列表
- HomePage Android 端使用 PageView + PageController 实现侧滑导航

### 修复
- 修复 Android 9+ 定位授权失败的问题，添加 ipapi.co HTTPS fallback 接口
- 修复 AndroidManifest 缺少 `usesCleartextTraffic` 导致 HTTP 请求被阻止的问题
- 修复重复查询同一城市只有首次提示的问题，移除冗余缓存提示弹窗
- 修复浅色主题下天气文字颜色不清晰的问题，雨/雪类天气文字加深
- 修复 windows_home_page.dart 中重复定义 `_showLocationPrompt` 方法导致编译错误的问题
- 修复 IP 定位全部失效的问题：ip-api.com / ipapi.co / ip.sb 在国内网络环境下均不可用，新增 pconline.com.cn 作为首选定位 API（UTF-8 JSON，国内稳定可用）

---

## 版本 0.0.20 (2026-07-29) 品牌重塑与液态玻璃全面升级

> 应用正式定名为「云栖枝」，英文名 Cloudbranch，全面启用全新品牌标识。Windows 与 Android 采用统一 UI 模板，Android 专属适配。所有 UI 全面采用毛玻璃风格。

### 品牌重塑
- 应用中文名正式定为「云栖枝」，英文名 Cloudbranch（首字母大写）
- 所有界面标题、关于页面、版本信息中的名称统一更新
- 平台版本号分离管理，Windows 与 Android 独立版本控制
- 代码中所有相关标识符统一迁移至 Cloudbranch 命名空间
- 启动画面、窗口标题、安装包名称全面采用云栖枝品牌
- 全新应用图标，采用云栖枝品牌 Logo 设计，覆盖 Windows（256/48/32/16px 多尺寸）与 Android（mdpi~xxxhdpi 全密度）
- Logo 重绘并重新嵌入应用图标，覆盖全平台

### 毛玻璃 UI 全面应用
- 所有 UI 组件（包括弹窗、对话框、菜单）统一采用毛玻璃风格
- GlassCard 可复用组件全面替换原有卡片样式
- buildGlassDialog 统一玻璃态弹窗包装器，所有弹窗使用毛玻璃效果
- GlassButton 玻璃按钮组件，带点击缩放动画
- 背景、AppBar、Drawer 等半透明组件统一采用液态玻璃风格

### Windows 液态玻璃专属优化
- 窗口级磨砂效果：配合系统 Mica/Acrylic 效果，窗口背景与系统壁纸联动
- 渲染降级策略：Windows 端使用 BackdropFilter 渲染，模糊半径限制 sigmaX: 6 / sigmaY: 6 保证性能
- 悬停反馈：鼠标悬停玻璃卡片时轻微上浮（translateY -4px）+ 边框高光变亮（透明度 0.15 → 0.4），200ms 过渡
- 点击反馈：InkWell + splashColor 水波纹效果，替代移动端按压缩放
- 键盘快捷键：玻璃组件支持 Ctrl+数字快捷键切换，快捷键提示以半透明 Chip 显示在卡片右下角
- 窗口布局：默认尺寸 1425×815，支持自由拉伸，最小尺寸限制防止玻璃元素挤压变形
- 右键菜单：玻璃卡片右键弹出上下文菜单（复制/刷新/置顶），菜单背景使用半透明毛玻璃效果
- 玻璃卡片使用 Flexible/Expanded 自适应布局，宽屏下展示更多内容

### 主题系统修复
- 修复切换主题后字体不同步修改、看不清字体的问题
- 修复深色模式下天气图标也是深色、看不清的问题
- 统一字体颜色管理，通过 AppTheme.textPrimary/textSecondary 全局分发

### 设置页面优化
- 移除设置二次确认，修改后直接在下方提示"设置已保存"
- 设置-单位：调整显示单位后查询天气根据设置改变，采用本地数据转化计算方案（减少 API 调用）
- 支持 ℃/℉ 温度单位切换、km/h 与 m/s 风速单位切换

### 定位与缓存系统
- 打开应用后请求位置权限或网络 IP 定位，允许后默认查询当地天气
- 不允许定位时提示可在设置中开启默认查询当前位置天气
- 设置-常规中添加「默认查询当前位置」开关（位于历史记录设置上方）
- 双端缓存：关闭应用后保存上一次查询的天气数据，下次打开时直接显示
- 查询间隔检测：上一次查询时间与当前不超过 30/60 分钟时弹窗提示查询间隔较短
- 历史记录标注：查询城市后在后台保存数据，历史记录标注「已查询」
- 覆盖查询后自动清理上上次查询数据，防止内存沉积

### Android 专属 UI 适配
- 添加 SystemChrome edge-to-edge 支持，实现 Android 15+ 边到边显示
- 状态栏透明、系统导航栏深色配色，与应用主题一致
- 底部导航栏高度适配 Android 触控标准（72px vs 64px）
- 搜索栏、查询按钮等组件针对 Android 触控目标优化
- 玻璃态模糊度降低至 3.0（Android 性能优化），不透明度提高至 0.35
- Android 禁用侧边抽屉，改用底部弹出菜单（BottomSheet）
- 内容全宽布局（无最大宽度限制），充分利用手机屏幕空间
- 导航栏显示文字标签，提升可识别性
- 修复 Android 端切换主题后部分区域仍保留深色 UI 的问题

### Android 液态玻璃专属优化
- 渲染引擎：Android 端调用原生 Skia 渲染器获得最佳性能
- 性能约束：列表滚动时禁止对列表项玻璃容器应用动态模糊动画，BackdropFilter 模糊半径限制 sigma ≤ 10
- RepaintBoundary 包裹静态玻璃组件，减少重绘区域，保证 120fps 稳定
- 点击反馈：液态收缩动画 Transform.scale 1.0 → 0.97 → 1.0，配合 Curves.easeOutBack
- 长按反馈：光影涟漪效果，ShaderMask/AnimatedContainer 渐变高光从按压点扩散
- 沉浸式布局：状态栏和导航栏设为透明，内容延伸到刘海/挖孔区下方
- 深色模式适配：Android 12+ 支持 Material You 动态取色，玻璃色调从 ColorScheme.secondaryContainer 提取
- 手势兼容：边缘侧滑返回时玻璃背景模糊度随滑动距离动态变化（0 → 20px），提供物理阻尼感

### 技术改进
- 创建 PlatformConfig 配置中心，集中管理平台差异常量
- 移除所有硬编码的平台判断，通过 PlatformConfig 统一分发
- 修复常量表达式与运行时 getter 的兼容性问题
- WillPopScope 升级为 PopScope（Flutter 3.12+ API）
- Android 硬件返回键处理：搜索建议 → Tab 切换 → 退出应用
- 天气缓存添加大小限制（20 条），防止内存无限增长
- 硬编码图标尺寸替换为 PlatformConfig 动态适配

### 构建修复
- Android NDK 版本固定为 26.1.10909125
- 添加 JDK 25 兼容 JVM 参数
- 配置 PUB_CACHE 和 GRADLE_USER_HOME 解决中文路径编译问题

---

## 版本 0.0.19 (2026-07-28) 毛玻璃

> 🔥 **UI全面重新设计，采用毛玻璃风格。**

### 视觉重做
- 背景从亮紫粉渐变改为深色渐变（#0D1117→#161B22→#1A1F35→#16213E），提升文字对比度
- 玻璃卡片参数全面调整：不透明度 0.12→0.28，模糊度 15→6，文字更清晰易读
- 主题色彩从紫色系（#A78BFA）统一改为蓝色系（#4A90D9），更沉稳专业
- AppBar、Drawer、Dialog 等所有半透明组件统一采用液态玻璃风格
- 按钮和交互元素同步更新为高不透明度蓝色调

### 组件化
- 创建 GlassCard 可复用玻璃卡片组件，支持自定义圆角、模糊度、不透明度
- 创建 GlassButton 玻璃按钮组件，带点击缩放动画
- 创建 buildGlassDialog 统一玻璃态弹窗包装器

### 技术改进
- 所有页面（home_page、weather_result、favorite_list、settings_page、about_page、help_page、changelog_page）统一引用 GlassCard 组件
- 颜色常量集中管理，主题色通过 ColorScheme 全局分发

---

## 版本 0.0.18 (2026-05-15) 天气查询更新

### 新特性
- 城市搜索框：输入关键词自动调用和风天气城市搜索API，展开联想城市名称，支持选择搜索结果快速查询
- 历史记录整合：最近搜索历史移至收藏城市栏下半部分，与收藏城市合并显示，上半部分为收藏城市、下半部分最近搜索
- 搜索框交互优化：搜索框添加清除按钮和加载指示器，实时显示搜索状态

### 修复
- 修复每小时天气预报显示过期小时的问题，过滤掉已过去的时间段，只显示未来24小时
- 修复温度趋势图底部日期标签过小不清晰的问题，加大字号加粗
- 修复回车键绑定问题，回车直接重新查询当前城市天气而非走缓存
- 修复城镇级城市（如龙坪镇）搜索失败的问题，支持模糊匹配上级行政区

### 优化
- 版本名称移至更新日志版本号旁边显示（如：v0.0.18 — 天气查询更新）
- 标题栏去掉版本名称，保持简洁
- 设置持久化增加保存日志，便于调试存储问题

### 技术改进
- WeatherApiService 新增 searchCities 方法，返回城市名称+行政区划列表
- AppState 新增 searchCities 桥接方法
- 搜索框使用 Overlay + CompositedTransformFollower 实现悬浮下拉建议框
- FavoriteList 重构为上下双区域布局（收藏城市 + 最近搜索）
- ChangelogService 解析器支持版本名称提取

---

## 版本 0.0.17 (2026-05-14)

### 修复
- 修复了设置页面未保存时退出导致空指针异常的问题
- 修复了应用重启后无法正常启动的问题
- **修复了 Web 首次启动时 `TypeError: Cannot read properties of undefined (reading 'getMajorVersion')` 错误**
- **修复了更新检查逻辑错误：构建号导致误报更新**
- **修复了收藏城市第二次打开后保存不下来的问题**
- **修复了 `FavoriteList` 在 Row 中无宽度约束导致崩溃的问题**

### 优化
- 增强了 `AppSettings.fromJson` 方法的健壮性，支持 null 输入和类型验证
- 添加了未保存更改检测和退出确认提示
- 改进了 `StorageService` 的空值检查和异常处理
- 添加了详细的日志记录便于调试
- **更新日志统一由 `CHANGELOG.md` 文件管理，应用内自动加载显示**
- **版本号集中管理，支持一键修改**
- **更新日期同步到关于页面**
- **删除冗余文件 `constants.dart`，统一使用 `environment.dart`**
- **删除未使用的 `_resultHtml` 字段**
- **所有核心代码添加注释，便于后续维护**
- **版本比较逻辑优化：支持主版本号、构建号、beta/稳定版智能判断**
- **检查更新功能添加弹窗提示，无更新时也明确提示用户**
- **收藏城市支持持久化保存，重启后可正常恢复**
- **天气数据缓存机制：同一城市+查询类型永久缓存，只在刷新时更新**
- **更新日志支持醒目标志渲染**

### 技术改进
- 在 `SettingsPage` 中添加了 `WillPopScope` 拦截返回操作
- 添加了 `_hasUnsavedChanges` 标志跟踪设置变化
- 在 `AppSettings` 中添加了 `_parseFavoriteCities` 静态方法处理数据解析
- 在 `StorageService` 中将 `_prefs` 改为可空类型并添加空值检查
- 创建 `ChangelogService` 服务解析 `CHANGELOG.md` 文件
- 更新 `Environment` 类添加 `buildDate` 字段集中管理更新日期
- `majorVersion` 改为初始化时提前计算缓存，避免 Web 模块加载竞态
- `getMajorVersion` 增强：支持处理 `v` 前缀、`+` 构建号、`-` 预发布后缀
- `compareVersions` 重构：三层决策（主版本→beta判断→构建号），支持语义化版本比较
- `FavoriteList` 改为自包含的 `Consumer<AppState>`，解耦外部传参
- `AppState` 添加 `_weatherCache` 缓存 Map，同一城市+查询类型永久缓存数据
- `SettingsPage` 添加 `didChangeDependencies` 重新加载最新 `settings`，确保收藏城市同步
- `FavoriteList` 包裹 `SizedBox(width: 200)` 限定宽度，防止 ListView 无限延展崩溃
- 重写 `ChangelogService`，支持醒目标志类型解析
- 更新 `ChangelogPage`，支持醒目标志渲染，显示橙色警告框

---

## 版本 0.0.16 (2026-05-11) 🔥 重磅更新

> **重构整个项目，采用 Flutter 架构运行，跨平台支持 Windows / Web / 移动端。**

### 重构
- **全新 Flutter 架构**：从 PyQt5 全面迁移至 Flutter 框架
- 跨平台支持：Windows 桌面端 + Web 浏览器 + 移动端（Android / iOS）
- 使用 Provider 状态管理替代原有的回调式状态同步
- Material Design 3 设计语言，现代化 UI 体验

### 修复
- 修复了初始化时 `TypeError: Cannot read properties of undefined (reading 'AppSettings')` 错误
- 添加了初始化加载状态，防止 UI 在数据准备好之前访问未定义的属性

### 优化
- 为代码添加了详细文档注释，便于后续维护
- 增强了存储服务的 JSON 解析健壮性
- 添加了空值检查和类型检查

### 配置
- 更新了项目构建脚本
- 添加了 Windows 构建配置说明

### 技术改进
- 在 `AppState` 中添加了 `_isInitialized` 标志追踪初始化状态
- 在 `main.dart` 中添加了 `_InitializationWrapper` 组件处理加载状态
- 在 `StorageService` 中增强了 JSON 解析的错误处理

---

> ⚠️ **Weather App (PyQt5)** 以下为旧架构更新日志

## 版本 0.0.15 (2026-01-19) 🔥 旧架构终版

### 优化
- 智能更新提示，区分大版本和小版本更新
- 修复了版本比较逻辑，确保本地版本大于云端版本时不会错误检测到更新
- 统一了版本比较逻辑，后台检查和手动检查使用相同的比较方法

### 修复
- 天气查询不能显示图标的BUG
- 修复了更新日志格式不正确的问题

### 技术改进
- 能够处理各种格式的版本号，包括标准格式、预发布版本
- 优化了版本解析，支持各种前缀和后缀

---

## 版本 0.0.13 (2026-01-19)

### 新增功能
- 添加后台静默检测更新功能，应用启动后自动检查更新
- 有新版本时自动显示更新提示，包含更新日志
- 无新版本时自动静默终止检查，不打扰用户
- 优化版本号显示，窗口标题动态显示当前版本

---

## 版本 0.0.12 (2026-01-19)

### 新增功能
- 为天气显示添加本地和风天气图标，包括实时天气、每小时预报和每日预报
- 实时天气中显示64px大小的天气图标，与温度显示协调
- 每小时预报中显示32px大小的天气图标，与小时卡片协调
- 每日预报中为白天和夜晚天气分别显示32px大小的天气图标

---

## 版本 0.0.11 (2026-01-19)

### 优化
- 优化UI布局，使其更简洁美观
- 实现完整的设置页面，包含主题、常规、单位、收藏城市设置
- 优化查询页面布局，添加当前城市/收藏城市选项卡
- 将查询类型改为按钮形式，支持实时/每小时/本周天气
- 添加查询时间显示，保证实时性
- 实现回车键刷新功能
- 设置页面嵌入应用，取消弹窗形式
- 关于软件和更新日志改为弹窗形式
- 输出框右上角固定显示查询时间
- 刷新按钮功能调整为刷新当前天气
- 修改设置页面样式，减少绿色使用，使用更柔和的颜色

### 修复
- 修复设置页面太小问题，使其覆盖整个页面
- 修复在设置页面时查询不会跳转到天气显示的问题
- 修复设置页面缺少返回按钮，无法退出的问题
- 修复设置页面保存按钮递归调用导致崩溃的问题
- 修改查询时间颜色为更明显的红色
- 调整收藏城市列表占比，减少空间占用
- 修复查询操作逻辑，每次查询都会自动返回天气显示界面

---

## 版本 0.0.1 (2026-01-19)

### 新增功能
- 使用PyQt5重构页面，优化并重新制作软件
- 将菜单功能分离为独立文件，便于维护
- 将窗口启动位置设置为屏幕中心

### 修复
- 修复API文档菜单崩溃问题

---

## 历史版本

### 版本 0.0.0.6
- 优化版本历史显示（滚动查看）
- 添加动态窗口标题

### 版本 0.0.0.5
- 调整系统菜单至首位
- 新增城市输入空值校验
- 添加天数空值校验

### 版本 0.0.0.4
- 菜单新增退出功能
- 新增ttkbootstrap主题美化
- 优化天气图标集成方式

### 版本 0.0.0.3
- 新增回车键立即查询功能
- 关于页面添加B站主页

### 版本 0.0.0.2
- 修改了分辨率的支持
- 优化查询按钮视觉效果

### 版本 0.0.0.1
- 新增菜单栏功能
- 增加系统设置界面
